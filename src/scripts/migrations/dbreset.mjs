import { createClient } from '@libsql/client'; const db = createClient({ url: process.env.TURSO_URL, authToken: process.env.TURSO_TOKEN }); for (const t of ['predictions_v2','historical_matches','fixture_odds','fixtures','teams','tournaments']) { try { await db.execute('DELETE FROM ' + t); console.log('Cleared:', t); } catch(e) { console.log('Skip:', t, e.message); } } console.log('Done'); process.exit(0);
